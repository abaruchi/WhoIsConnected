[gmail]
ACCESS_KEY = <aws access key>
SECRET_KEY = <aws access key>
SUBJECT = <subject to be used by the daemon>
DEST = <destination email>
SENDER = <sender email>

[dhcp]
LEASE_FILE = <dhcp.leases file full path>
IGNORE_HOSTS = <list of hostname, comma separated, to ignore>

[network]
MAC_URL = http://macvendors.co/api/%s

[daemon]
PROBING_MIN = <probing frequency in minutes>
